# CLAUDE.md — правила работы над проектом MiniCCompiler

Компилятор Mini-C → Macro-11 для УКНЦ (Электроника МС-0511, PDP-11).
Написан на C# (WinForms). Этот файл — инструкция для Claude в будущих сессиях.

Всё, что ниже про язык и рантайм, сверено с исходниками (Lexer.cs, Parser.cs,
CodeGen*.cs) и проверено на эмуляторе. Если код и этот файл разойдутся —
прав код, а файл надо править.

---

## ГЛАВНЫЕ ПРИНЦИПЫ (соблюдать всегда)

### 1. Думать до кода
Явно формулировать допущения, указывать альтернативные интерпретации и не
начинать работу, если есть сомнения. При размытом запросе («улучши этот
handler») — спросить, что именно улучшать (производительность, читаемость,
логирование), а не сразу вносить изменения.

### 2. Простота вперёд
Писать минимально возможный код, решающий задачу, без лишних абстракций,
«гибкости» или обработки невозможных сценариев. Если 200 строк можно
сократить до 50 — переписать. Никаких решений «на всякий случай», если они
не запрошены.

### 3. Хирургические правки
Изменять только то, что нужно, и удалять только свой мусор. Не рефакторить
соседние модули «заодно», если это не связано с задачей. Избегать «drive-by»
правок, меняющих код без явной необходимости.

### 4. Не додумывать допущения
Не принимать неверные предположения молча. Если задача неоднозначна —
уточнить у пользователя, а не гадать. Не выбирать интерпретацию и идти
вперёд без подтверждения, не скрывать противоречия.

---

## КАК МЫ РАБОТАЕМ

- **Рабочий цикл:** пользователь ставит задачу → Claude правит файлы →
  кладёт в outputs **полный файл целиком** (не диффы, не фрагменты) →
  пользователь скачивает, заменяет в `D:\UKNC\CompMacro11\`, жмёт
  **Ctrl+Shift+B** в Visual Studio, затем **F5** (компиляция Mini-C) и
  «▶ Эмулятор» (запуск в UKNCBTL) → говорит, работает или нет.
- **Проверку «работает / не работает» делает пользователь.** Собрать проект
  у себя Claude не может: в контейнере нет dotnet SDK и отключена сеть, так
  что ни `apt-get`, ни NuGet не отработают. Не выдавать непроверенный код за
  проверенный и не городить обходные инструменты без просьбы.
- **Скриншоты — только когда картинка = вопрос** (артефакты, цвета, мусор на
  экране). Результаты тестов, ошибки, «работает/не работает» — текстом.
  Скриншот стоит ~1000+ токенов, текст — десяток. Зато если скриншот уже
  прислан, из него можно снять точные RGB — так была найдена ошибка в
  таблице цветов.
- **Если у пользователя ошибка, а у Claude всё сходится** — это рассинхрон
  файлов (не заменил / не пересобрал). Проверять Output в VS на «Сборка успешна».
- **Тон:** на русском, кратко, по делу, признавать ошибки честно.
- **Папка `Samples`** — пользователь наполняет сам отлаженными проектами.
  Claude туда ничего не добавляет без явной просьбы.
- Исходники Mini-C имеют расширение `.mc` (у проекта это `main.mc`).

---

## ЯЗЫК Mini-C — что поддерживается

Типы: `int` (16 бит со знаком), `void`, `bool` (true/false).
Массивы 1D/2D. Константы: десятичные, hex (`0x1F`), отрицательные.
Управление: if/else, while, for, do-while, switch/case, break, continue, return.
Арифметика: `+ - * / %`. Сравнения. Логика `&& || !` (короткое замыкание).
Биты: `& | ^ ~ << >>`. Присваивания: `= += -= *= /= %= &= |= ^= <<= >>=`.
Тернарный: `cond ? a : b` (вложенный работает). Функции с рекурсией.
Прототипы функций (`int f(int x);` — тело ниже). `int x, y;` (глоб. и лок.).

**НЕТ:** goto, char[]-строки, запятая в for (`i++,j--`), указатели, struct.

### Подводные камни грамматики (частые ошибки)
- **Инкремент только ПОСТФИКСНЫЙ.** `i++`, `i--` работают; `++i` и `--i` —
  синтаксическая ошибка. Унарного `+` тоже нет (`x = +5;` не соберётся).
- **Символьных констант нет.** `'A'` — ошибка лексера. Только числовой код
  (`if (k == 89)` вместо `if (k == 'Y')`).
- **Авторазмер массива `[]` допустим ТОЛЬКО в параметре функции.**
  `void f(int buf[])` — можно; `int a[] = {1,2,3};` — ошибка, размер обязателен.
- **Размер массива — только целочисленный литерал.** `int a[10]` можно,
  `int a[2*3]` нельзя.
- **Инициализатор массива — только целочисленные литералы**, вложенные скобки
  просто уплощаются. `int a[3] = {1, f(2), 3};` не соберётся.
  Висячая запятая `{1,2,}` — тоже ошибка.
- **Метка case — только положительный литерал.** `case -1:` не соберётся.
- **В for-init можно объявить переменную, но только `int`:** `for (int i = 0; ...)`.
- Строковые литералы понимают escape `\n \r \t \0 \" \\`; остальные `\x`
  отдают сам символ.

### Препроцессор (Lexer.Preprocess)
```c
#define WIDTH 320            // подстановка КАК ЕСТЬ, без скобок
const int LIMIT = 100;       // подстановка В СКОБКАХ: (100)
```
Строки определений заменяются пустыми, нумерация строк сохраняется.

Две ловушки:
- **Вложенные `#define` не раскрываются.** `#define W 320` + `#define HALF W/2`
  даст в тексте `W/2`, и `W` останется неопознанным идентификатором: проход
  по словарю однократный, в порядке добавления.
- **`const int` нельзя использовать как размер массива** — подставится `(10)`,
  а размер требует голого литерала. Для размеров только `#define`.
- `#define` без скобок ломает приоритет: `#define HALF 320/2` в выражении
  `HALF*2` даст `320/2*2`. Оборачивать значение в скобки самому.

### Кириллица
**Русский текст в строках РАБОТАЕТ** — `CodeGen.Koi8Uknc()` переводит символы
в коды знакогенератора (строчные 192..223, заглавные 224..255).
**Букв `Ё`/`ё` в знакогенераторе нет**, как и прочего не-ASCII: подставится `?`.
Писать «черный», «зеленый», «желтый». Подробности — в `UKNC_CODES.md`.

---

## ДВА РЕЖИМА РАНТАЙМА

`CodeGen.Mode`: `RtMode.Cpu` (по умолчанию) или `RtMode.Game`.
Переключается тумблером в IDE (`Form1._gameMode`). От режима зависит, какие
встроенные функции доступны — **это самый частый источник ошибок компиляции**.

- **ЦП** — векторная графика силами центрального процессора, 4 цвета, 4 режима.
  Запрещены все ПП-функции: `ppu_init`, `pp_init`, `pp_point`, `pp_line`,
  `pp_sprite`, `pp_stop`, `pp_spr`, `pp_spr_q`, `pp_vspr`, `pp_blit`,
  `pp_flush`, `pp_qhead`, `pp_qtail`, `pp_peek`, `vpoke`, `vload`,
  `pp_upload`, `pp_sprm`, `pp_upaddr`.
- **Game** — 320×264, связка ЦП+ПП, только спрайтовая графика.
  Запрещены: `point`, `line`, `rect`, `fill_rect`, `fill_dither`, `circle`,
  `box`, `sin256`, `cos256`.

Если программа использует функции из обоих списков — она не соберётся ни в
одном режиме.

---

## ВСТРОЕННЫЕ ФУНКЦИИ (сверено с `_builtins` и `GenBuiltin`)

Экран и время: `init(mode)`, `cls(mode)` — mode 0..3, это режим экрана, а не
цвет; `pause()` без аргументов; `vsync()`, `getTimer()`, `random(n)` → 0..n-1.

Текст: `print_char(c)`, `print_nl()`, `print_int(n)`, `print_str(s)`,
`print(x)`, `printnum(n)`, `printf(fmt, ...)` (`%d %c %s`, `\n`),
`print_buf(buf, n)`, `str_to_buf(s, buf)`, `gotoxy(x, y)`,
`setTextColor(c)`, `setPlaceColor(c)`, `setCursorColor(c)`.

Графика ЦП: `point(x,y,c)`, `line(x1,y1,x2,y2,c)`, `rect(...5)`,
`fill_rect(...5)`, `fill_dither(x,y,w,h,pattern,fg,bg)` — **7 аргументов**,
`circle(x,y,r,c)`, `box(...5)`.
**`fill_gradient` не существует** — раньше значилась здесь по ошибке.

Спрайты: `sprite(x,y,w,h,ptr)`, `spriteOr(x,y,w,h,ptr)` — 5 аргументов,
`spr(x,y,id)` — 3. Флипов нет.

Периферийный процессор (режим Game): `ppu_init()`, `pp_init()`, `pp_stop()`,
`pp_flush()`, `pp_point(x,y,c)`, `pp_line(...5)`, `pp_sprite(...5)`,
`pp_spr(x,y,id)`, `pp_spr_q(...3)`, `pp_vspr(...3)`, `pp_blit(...6)`,
`pp_peek(addr)`, `pp_qhead()`, `pp_qtail()`, `vpoke(addr,val)`, `vload(...3)`.

Спрайты в ОЗУ ПП (быстрый вывод): `pp_upload(ptr)` → номер спрайта,
`pp_sprm(x,y,номер)`, `pp_upaddr(номер)` → адрес в ОЗУ ПП (для отладки).

Ввод: `getkey()` (неблок.), `waitkey()` (ждёт).
Математика: `abs`, `min`, `max`, `clamp(v,lo,hi)`, `sin256(a)`, `cos256(a)`.
Диск RT-11: `fload`, `fsave`, `fdelete`, `frename`, `mkdir`, `getcwd`,
`chdir`, `file_info` (см. `CodeGen_DiskIO.cs`).

Возврат значения у `void`-функций **не проверяется**: `x = print_int(5);`
скомпилируется, но в R0 окажется мусор.

---

## СПРАЙТЫ В ОЗУ ПП

```c
int back = pp_upload(sprite_2);   // ДО pp_init()
pp_init();
...
pp_sprm(x, y, back);
```

`pp_upload` кладёт спрайт в память ПП и возвращает **номер** (не адрес).
Резидент читает такие спрайты напрямую — вчетверо меньше команд на октет,
чем при чтении из памяти ЦП. Вызывать обязательно до `pp_init()`.

- Номер всегда нечётный: адреса в памяти ЦП чётные, и по этому биту
  система отсечения различает, откуда рисовать. Смешивать `pp_spr` и
  `pp_sprm` в одном кадре можно.
- До 16 загруженных спрайтов, при переполнении `pp_upload` вернёт 0.
- Память в ПП не освобождается — спрайты живут всю программу.
- `pp_upaddr(номер)` отдаёт адрес в ОЗУ ПП; нужен только чтобы посмотреть,
  что вернул ПП, когда что-то не рисуется.

---

## КЛЮЧЕВЫЕ ФАКТЫ УКНЦ

- **Окт/дес:** число БЕЗ точки = восьмеричное, С точкой = десятичное.
  `#33` = 27 десятичное, `#33.` = 33. Это источник многих багов!
- **Управляющие коды (десятичные):** ESC=27, CR=13, LF=10, FF=12 (очистка!),
  'Y'=89, '0'=48, пробел=32. Перевод строки = CR(13)+LF(10).
  Очистка экрана из Mini-C — `print_char(12)`; графический `cls()` для этого
  не годится, он про VRAM.
- **Вектор кадрового прерывания = 100** (50 Гц). Регистра LTC @#177546 на ЦП
  УКНЦ НЕТ — обращение к нему = Trap to 4! vsync/getTimer работают через
  перехват вектора 100 с цепочкой к монитору.
- **Экран** 320×264 (режим 0/2) или 640×264 (режим 1/3), строка VRAM = 80 слов.
- **Видеопорты:** @#176640 адрес, @#176642 данные. Консоль @#177564 статус,
  @#177566 данные. Бит0 = ЛЕВЫЙ пиксель.
- **`gotoxy(x, y)`** — первым аргументом колонка, но в escape-последовательность
  рантайм шлёт СТРОКУ первой (`ESC Y y+32 x+32`, VT52). Менять местами при
  правках `RTGOTO` нельзя.
- **Цвета текста `setTextColor` 0..7:** 0=чёрный 1=синий 2=зелёный 3=голубой
  4=красный 5=малиновый 6=жёлтый 7=белый.
  **Биты: бит0=синий, бит1=зелёный, бит2=красный** (проверено попиксельно).
- **`setPlaceColor(c)`** красит знакоместа, выводимые ПОСЛЕ вызова; уже
  нарисованное не перекрашивается. Так рисуются цветные «плитки» пробелами.
- **Цвета графики (cls/point/line 0..3)** — зависят от аппаратной палитры,
  ОТЛИЧАЮТСЯ от текстовых.
- **Знаковое деление:** DIV требует SXT для расширения знака:
  `MOV R0,-(SP) / TST R1 / SXT R0 / DIV (SP)+,R0` (для % добавить MOV R1,R0).
  ASR-деление степеней 2 для отрицательных требует коррекции +n-1 (C-семантика
  «к нулю» vs ASR floor).
- **Формат спрайта:** ширина в единицах по 8px (PixelWidth = Words*8),
  2 бита/пиксель (план0 = младший байт слова, план1 = старший). Подключение
  в код комментарием `// sprite: имя` — редактор экспортирует массив сам.
- **Текст (через 177566) и графику (VRAM) НЕЛЬЗЯ мешать** в одной программе.
- **Соглашение о вызовах:** после пролога `MOV R5,-(SP)/MOV SP,R5/MOV R0,-(SP)`
  первый аргумент = `4.(R5)`, второй = `6.(R5)`, третий = `10.(R5)`.
- **В резиденте ПП не рассчитывать на регистры после вызова чужой
  подпрограммы** — отправка массива параметров затирает R2 и R3, запись
  октета под маской R3 и R5, сдвиг и вывод строки R0, R1, R5. Всё нужное
  держать в ячейках. Подробности и симптомы — в `UKNC_CODES.md`.
- **Новый код резидента должен лежать между метками начала и конца
  резидента** — он копируется в ПП целиком по этим границам.

---

## АРХИТЕКТУРА ПРОЕКТА

Файлы C#: Lexer, Parser, AST, CodeGen (+ partial-части с рантаймом), Peephole,
Stdlib, Form1 (IDE), Spriteeditor, Project, ProjectDialogs, DskImage,
DskDialog, Program.

- CodeGen разбит на partial-классы: `CodeGen.cs` (логика и `GenBuiltin`),
  `CodeGen_RuntimeCpu.cs` (`EmitRuntimeCpu`), `CodeGen_RuntimeGame.cs`
  (`EmitRuntimeGame`), `CodeGen_DiskIO.cs` (файловые операции RT-11).
  Все — `public partial class CodeGen`. Выбор рантайма — по `Mode`.
- csproj — старый формат (файлы перечислены явно): новый файл нужно добавлять
  в csproj руками.
- **Спрайты:** редактор сохраняет в путь, синхронизированный с тем, откуда
  читает компилятор (проектный `sprites/sprites.spr` или автосейв). При
  создании редактора вызывается UpdateSpritesPath(). Подключение — `// sprite:`.
- **Проект** (`Project.cs`) — папка с `.pkc` (свой мини-JSON), `main.mc`,
  подпапками `sprites/` и `build/`. Настройки машины (путь к эмулятору,
  последний проект) лежат в AppData, а не в проекте.

---

## РЕПОЗИТОРИЙ

GitHub: https://github.com/Nidden/MiniCCompiler
Перед началом работы делать `git pull`, чтобы не расходились ветки.
Пользователь на `D:\UKNC\`, проект в `D:\UKNC\CompMacro11\`, UKNC_CODES.md и
CLAUDE.md в корне.

---

## СПРАВОЧНИКИ

- `UKNC_CODES.md` — окт/дес, управляющие коды, escape-последовательности,
  таблица цветов, кириллица, видеопорты, соглашение о вызовах.
- Справка по языку — кнопка `?` в IDE.

---

## ИЗВЕСТНЫЕ РАСХОЖДЕНИЯ КОДА И ЗАДУМКИ (не починено)

- Авторазмер массивов заявлен, но `ParseArrayDims` разрешает пустой `[]`
  только в параметре функции — `int a[] = {1,2,3};` не собирается.
- `const int N = 10;` подставляется как `(10)` и потому не годится в размер
  массива, хотя выглядит как самое очевидное применение.
- Вложенные `#define` не раскрываются (однопроходная подстановка).
